// const axios = require('axios');
// const whois = require('whois-json');

// const checkDomainExpiration = async (url) => {
//   try {
//     const domainInfo = await whois(url);
//     const expirationDate = new Date(domainInfo.expires);
//     return expirationDate;
//   } catch (error) {
//     console.error('Error checking domain expiration:', error);
//     return null;
//   }
// };

// module.exports = checkDomainExpiration;
