// const axios = require('axios');

// const checkKeyword = async (url, keyword) => {
//   try {
//     const response = await axios.get(url, { timeout: 5000 });
//     return response.data.includes(keyword);
//   } catch (error) {
//     console.error('Error checking keyword:', error);
//     return false;
//   }
// };

// module.exports = checkKeyword;
